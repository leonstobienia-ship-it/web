# V2.7A.4B - Aprovacao Por Diretoria Como Alcada Superior

Data: 2026-06-09

Branch: `dev/v2.3-sharepoint-integracao`

## Status

Regra implementada localmente para a acao:

`AprovarCompra`

Validacao manual fechada na V2.7A.4E: Leon confirmou visualmente que o snapshot permaneceu vinculado apos a aprovacao. A regra de Diretoria como alçada superior fica validada manualmente para o item `11`, sem liberar uso amplo de producao.

Codex nao conectou ao SharePoint, nao publicou pacote, nao instalou app, nao alterou listas/permissoes/dados, nao executou escrita operacional, nao executou teste funcional no tenant e nao iniciou Power Automate.

## Decisao Formal

Leon definiu que Diretoria pode aprovar como alcada superior quando o aprovador previsto no snapshot for outro usuario, desde que a aprovacao seja explicita, restrita e auditavel.

Essa regra nao e bypass generico.

Ela se aplica somente a `AprovarCompra` e somente quando todas as condicoes obrigatorias forem satisfeitas.

## Contexto Do Item 11

Pre-validacao manual anterior:

- item `11`;
- acao `AprovarCompra`;
- status atual `Aguardando aprovação`;
- status destino `Aprovada para compra`;
- snapshot atual `3` / `SNAP-V2.7A-TESTE-11-20260609025123`;
- aprovacao necessaria `Sim`;
- valor analisado `R$ 6.720,00`;
- regra `alc-v23b-teste-compra-ate-20000`;
- aprovador previsto/base/efetivo do snapshot `V2.3B-TESTE - Gustavo`;
- usuario atual `V2.3B-TESTE - Leon / Diretoria`;
- transicao permitida;
- campo previsto `StatusdaRequisi_x00e7__x00e3_o`;
- bloqueio anterior `USUARIO_NAO_E_APROVADOR`.

## Regra Implementada

A aprovacao de compra pode passar por um destes caminhos:

1. Aprovador direto:

- usuario atual corresponde ao aprovador base/efetivo do snapshot.

2. Alcada superior / Diretoria:

- status atual da requisicao e `Aguardando aprovação`;
- status destino e `Aprovada para compra`;
- `SnapshotAprovacaoCompra` existe e esta vinculado;
- snapshot tem regra/alçada e aprovador previsto legiveis;
- usuario atual esta ativo;
- usuario atual tem perfil principal `Diretoria`;
- usuario atual tem permissao para `AprovarCompra`;
- marcador `V2.7A-TESTE` esta confirmado no item;
- confirmacao manual exata esta configurada;
- historico operacional registrara a aprovacao por alcada superior.

## Diagnosticos

A pre-validacao passa a diferenciar:

- `APROVADOR_DO_SNAPSHOT_VALIDO`;
- `DIRETORIA_ALCADA_SUPERIOR_VALIDADA`;
- `USUARIO_NAO_E_APROVADOR`;
- `PERFIL_DIRETORIA_NAO_ATIVO`;
- `SNAPSHOT_OBRIGATORIO_AUSENTE`;
- `STATUS_NAO_ELEGIVEL_PARA_APROVACAO`;
- `STATUS_DESTINO_NAO_MAPEADO`;
- `TRANSICAO_NAO_PERMITIDA`.

Os dois primeiros sao diagnosticos de aprovacao, nao alertas bloqueantes.

## Pre-validacao Esperada Para Diretoria

Quando Leon/Diretoria executar a pre-validacao para o item `11`, a tela deve exibir:

- aprovador previsto `V2.3B-TESTE - Gustavo`;
- aprovador efetivo operacional `V2.3B-TESTE - Leon / Diretoria`;
- tipo de aprovacao `Alçada superior / Diretoria`;
- diagnostico `DIRETORIA_ALCADA_SUPERIOR_VALIDADA`;
- justificativa prevista:

`Aprovação por alçada superior: aprovador previsto V2.3B-TESTE - Gustavo; aprovador efetivo V2.3B-TESTE - Leon / Diretoria.`

`podeExecutar` so pode ser `true` se nao houver alertas bloqueantes.

## Execucao Futura

A execucao futura de `AprovarCompra` deve:

- revalidar o item por GET imediatamente antes da escrita;
- confirmar item ID, marcador, status atual e snapshot;
- confirmar Diretoria/alçada superior, se aplicavel;
- executar MERGE apenas no item configurado;
- alterar somente `StatusdaRequisi_x00e7__x00e3_o`;
- fazer GET posterior ao MERGE e confirmar preservacao de `SnapshotAprovacaoCompra`;
- gravar valor anterior `Aguardando aprovação`;
- gravar valor novo `Aprovada para compra`;
- registrar historico operacional somente se o snapshot for preservado;
- nao criar pedido;
- nao criar NF;
- nao programar pagamento;
- nao iniciar Power Automate;
- nao usar `DELETE`;
- nao usar `PATCH`.

Historico obrigatorio para alcada superior:

- acao `AprovarCompra`;
- item `Lista 02-11`;
- marcador `V2.7A-TESTE`;
- aprovador previsto `V2.3B-TESTE - Gustavo`;
- aprovador efetivo `V2.3B-TESTE - Leon / Diretoria`;
- justificativa `Aprovação por alçada superior / Diretoria`;
- origem `Webpart V2.7A.4B`;
- sem Power Automate.

## Idempotencia

Se o item ja estiver em `Aprovada para compra`, nenhuma nova escrita deve ser executada.

O estado deve ser registrado como validacao existente e a acao deve ficar bloqueada ate nova decisao formal.

## Proxima Etapa Manual

Com a confirmacao V2.7A.4E, a proxima etapa recomendada e preparar V2.7A.5 para `CriarPedidoCompra`, com validacao readonly previa da `Lista 03 — Pedidos de Compra`.

Nao executar a escrita se a tela nao mostrar:

- `DIRETORIA_ALCADA_SUPERIOR_VALIDADA`;
- tipo `Alçada superior / Diretoria`;
- `pode executar = sim`;
- snapshot antes/depois preservado quando houver execucao;
- alertas `-`.

## Registro V2.7A.4E

- data-base: 09/06/2026;
- commit da regra: `5142663`;
- commit de auditoria/correcao: `4c38b0c2496b7584138a4d36b6d5089c9d09e506`;
- item: `11`;
- marcador: `V2.7A-TESTE`;
- acao: `AprovarCompra`;
- status: `Aguardando aprovação -> Aprovada para compra`;
- snapshot preservado: `3` / `SNAP-V2.7A-TESTE-11-20260609025123`;
- historico criado: sim;
- HTTP escrita: `204`;
- pedido, NF e pagamento: nao criados;
- Power Automate: nao iniciado.
