# V2.7A.6A - Auditoria Readonly Lista 04 Notas Fiscais

Data: 2026-06-09

## Objetivo

Confirmar, sem escrita, a estrutura real da `Lista 04 - Notas Fiscais Recebidas` antes de preparar a acao `VincularNotaFiscal`.

Codex nao deve executar este script, nao deve conectar ao SharePoint, nao deve publicar pacote, nao deve alterar tenant/listas/dados/permissoes e nao deve iniciar Power Automate.

## Script Preparado

`scripts/sharepoint/12-auditoria-lista04-notas-fiscais-readonly.ps1`

O script:

- conecta ao site `https://enaccombr.sharepoint.com/sites/Equipe.Obras`;
- localiza a Lista 04 por GUID `25aa4447-193d-418a-8e71-9bfd8e9995da` ou nomes candidatos;
- le metadados da lista e campos;
- destaca campos de pedido, NF, documento, fornecedor, valor, vencimento, emissao, status, obra, centro, requisicao e anexos;
- gera JSON e Markdown em `reports/`;
- nao altera listas, campos, itens, permissoes ou dados.

## Execucao Manual Sugerida

```powershell
pwsh .\scripts\sharepoint\12-auditoria-lista04-notas-fiscais-readonly.ps1 `
  -Tenant "enaccombr.onmicrosoft.com" `
  -ClientId "<app-id-autorizado>" `
  -AuthMode DeviceLogin
```

Saidas esperadas:

- `reports/lista04-notas-fiscais-fields-readonly.json`;
- `reports/lista04-notas-fiscais-fields-readonly.md`.

## Confirmacoes Necessarias

- Campo real de vinculo com pedido de compra.
- Existencia ou ausencia de vinculo direto com requisicao.
- Fornecedor correto: `Fornecedor0` lookup ou `CNPJFornecedor` texto.
- Choices reais de `StatusdaConfer_x00ea_ncia`.
- Status inicial da NF.
- Choices reais de `TipodeNF`.
- Campos obrigatorios para criar/vincular NF.
- Obrigatoriedade de anexo, documento ou link da NF.
- Existencia de NF de teste vinculada ao pedido item `3`.

## Status De VincularNotaFiscal

`VincularNotaFiscal` continua bloqueada.

Nenhum TypeScript de execucao foi preparado nesta rodada porque a Lista 04 ainda depende de auditoria readonly especifica.
