# V2.7A.5 - Preparacao Do Teste Criar Pedido De Compra

Data: 2026-06-09

Branch: `dev/v2.3-sharepoint-integracao`

## Status

Preparacao tecnica e documental da acao:

`CriarPedidoCompra`

Execucao bloqueada ate auditoria readonly manual da `Lista 03 — Pedidos de Compra`.

Codex nao conectou ao SharePoint, nao publicou pacote, nao instalou app no tenant, nao alterou listas/permissoes/dados, nao executou escrita operacional, nao executou teste funcional no tenant e nao iniciou Power Automate.

## Contexto Validado

A V2.7A.4E registrou a aprovacao manual final:

- item `11`;
- marcador `V2.7A-TESTE`;
- status `Aguardando aprovação -> Aprovada para compra`;
- snapshot preservado `3` / `SNAP-V2.7A-TESTE-11-20260609025123`;
- aprovacao por Diretoria / alçada superior;
- aprovador previsto `V2.3B-TESTE - Gustavo`;
- aprovador efetivo `V2.3B-TESTE - Leon / Diretoria`;
- historico criado;
- HTTP escrita `204`;
- sem pedido, NF, pagamento ou Power Automate.

## Estrutura Local Conhecida Da Lista 03

Inventario readonly versionado:

- lista: `Lista 03 — Pedidos de Compra`;
- GUID: `18ca132a-c36a-42aa-9968-d87ecd547a79`;
- URL: `/sites/Equipe.Obras/Lists/Lista 03  Pedidos de Compra`.

Campos vistos no inventario:

| Campo | InternalName | Tipo | Observacao |
| --- | --- | --- | --- |
| Obra | `Obra` | Lookup | Lista 01 / `NomedaObra` |
| Centro de Custo | `CentrodeCusto` | Text | Disponivel |
| Fornecedor antigo | `Fornecedor` | Text | Campo legado |
| Fornecedor | `Fornecedor0` | Lookup | Lista 06 / `CNPJ_x002f_CPF` |
| Valor do Pedido | `ValordoPedido` | Currency | Disponivel |
| Data do Pedido | `DatadoPedido` | DateTime | Disponivel |
| Status do Pedido | `StatusdoPedido` | Choice | Choices nao documentadas localmente |
| Descrição do Pedido | `Descri_x00e7__x00e3_odoPedido` | Note | Disponivel |

## Bloqueios De Schema

A execucao nao foi liberada porque:

- `sharepoint/list-schema.json` nao documenta a estrutura operacional real da Lista 03;
- o inventario local nao confirmou o campo `SolicitacaoId` na Lista 03;
- nao esta claro se o vinculo com a solicitacao deve usar `SolicitacaoId` ou outro campo;
- existem dois campos de fornecedor: `Fornecedor` texto antigo e `Fornecedor0` lookup;
- choices reais de `StatusdoPedido` ainda precisam ser confirmadas;
- obrigatoriedade real de `Fornecedor`, `DatadoPedido`, `ValordoPedido`, `Obra` e `CentrodeCusto` precisa ser confirmada no tenant.

## Preparacao Tecnica

A webpart passa a aceitar `CriarPedidoCompra` no Property Pane para diagnostico.

A pre-validacao permanece readonly e deve bloquear a execucao com alertas enquanto a Lista 03 nao estiver confirmada.

Alertas esperados nesta fase:

- `CAMPO_SOLICITACAO_PEDIDO_NAO_MAPEADO`;
- `CAMPOS_PEDIDO_OBRIGATORIOS_AUSENTES`;
- `FORNECEDOR_OBRIGATORIO_AUSENTE`;
- `STATUS_PEDIDO_INICIAL_NAO_MAPEADO`.

O botao de escrita nao deve aparecer porque `podeExecutar` fica `false`.

## Condicoes Futuras Para Usar O Item 11

Antes de liberar escrita, a pre-validacao devera confirmar:

- item `11` encontrado na Lista 02;
- marcador `V2.7A-TESTE`;
- status `Aprovada para compra`;
- `SnapshotAprovacaoCompra` preenchido com `3` / `SNAP-V2.7A-TESTE-11-20260609025123`;
- valor de teste `6720`;
- tipo `Material`;
- quantidade e unidade presentes;
- descricao presente;
- centro de custo presente;
- obra presente;
- inexistencia de pedido anterior vinculado;
- usuario/perfil com permissao para `CriarPedidoCompra`;
- Lista 03 mapeada por GUID;
- campos obrigatorios reais disponiveis;
- historico previsto.

## Execucao Futura

Quando liberada em rodada propria, a execucao devera:

- revalidar o item `11` por GET imediatamente antes da escrita;
- confirmar marcador, status aprovado e snapshot;
- confirmar inexistencia de pedido anterior;
- criar item de pedido por POST na Lista 03;
- preencher somente campos confirmados;
- vincular a solicitacao conforme campo real confirmado;
- registrar historico operacional;
- nao alterar status da requisicao nesta etapa;
- nao criar NF;
- nao programar pagamento;
- nao iniciar Power Automate;
- nao usar `DELETE`;
- nao usar `PATCH`, salvo decisao tecnica posterior documentada.

## Proxima Acao Manual

Leon deve executar uma auditoria readonly manual da Lista 03 antes de qualquer tentativa de escrita, confirmando:

- campos obrigatorios;
- choices de `StatusdoPedido`;
- tipo e existencia do campo de vinculo com a solicitacao;
- campo correto de fornecedor;
- se ha pedido ja vinculado ao item `11`.

## Complemento V2.7A.5A

Foi preparado o script readonly:

`scripts/sharepoint/11-auditoria-lista03-pedidos-readonly.ps1`

Ele deve ser executado manualmente por Leon, fora do Codex, para gerar:

- `reports/lista03-pedidos-fields-readonly.json`;
- `reports/lista03-pedidos-fields-readonly.md`.

Enquanto esses resultados nao forem registrados, `CriarPedidoCompra` permanece bloqueado.
