# V3.5A.1 - Auditoria do contrato canonico da Nota Fiscal de Entrada

## Objetivo

Auditar a V3.5A canonica para confirmar o contrato local de Nota Fiscal de Entrada e Conta a Pagar inicial, sem avancar V3.5B.

## Base auditada

- Checkout auditado: `cf5ec85`.
- Tag no HEAD: `v3.5a-nota-fiscal-entrada`.
- Branch: HEAD destacado.
- Banco local permitido: PostgreSQL Docker `enac_erp_dev`.

Observacao: a branch local `main` foi encontrada apontando para outro commit no inicio da auditoria. Esta etapa nao move branch, nao altera historico, nao cria tag e nao faz push.

## Conclusoes do contrato

- A rota funcional canonica de Nota Fiscal de Entrada e `/notas-fiscais-entrada`.
- O alias historico local antigo nao deve ser registrado como rota funcional.
- A criacao/provisionamento de Nota Fiscal de Entrada gera Conta a Pagar com status `PROVISIONADA`.
- Conta a Pagar nasce com `valor_aberto` igual ao valor original.
- Nao ha endpoint funcional de programacao bancaria, liberacao financeira, pagamento ou baixa nesta versao.
- Nao ha `DELETE` fisico nos modulos auditados.
- Nao houve alteracao de arquitetura de alcadas.
- Nao houve criacao de novo modulo.

## Escopo auditado

- Backend HTTP principal.
- Modulos de Nota Fiscal de Entrada.
- Modulo de Conta a Pagar.
- Migrations locais relacionadas.
- Tipos e services do frontend ligados a notas fiscais e contas a pagar.
- Smokes de Nota Fiscal de Entrada e Conta a Pagar.
- Documentacao operacional da V3.5A.

## Ajustes aplicados

- Removido o registro funcional do alias antigo no roteador principal.
- Mantida a rota canonica `/notas-fiscais-entrada`.
- Ajustado smoke de contas a pagar para localizar conta provisionada existente pelo marcador `DEV_LOCAL_V3_5A` tambem em campos auxiliares.
- Atualizada a documentacao para registrar o contrato canonico da V3.5A.1.

## Validacoes obrigatorias

```powershell
cd server
npm.cmd run build
npm.cmd run migrate
npm.cmd run smoke:notas
npm.cmd run smoke:contas-pagar
cd ..
npm.cmd run web:build
npx.cmd tsc -p web/tsconfig.json --noEmit
npx.cmd tsc -p tsconfig.json --noEmit
```

Resultado da rodada em 2026-06-16:

- `npm.cmd run build` em `server`: aprovado.
- `npm.cmd run migrate` em `server`: aprovado, todas as migrations ja aplicadas.
- `npm.cmd run smoke:notas` em `server`: aprovado.
- `npm.cmd run smoke:contas-pagar` em `server`: aprovado.
- `npm.cmd run web:build` na raiz: aprovado, com aviso Vite de chunk acima de 500 kB.
- `npx.cmd tsc -p web/tsconfig.json --noEmit`: aprovado.
- `npx.cmd tsc -p tsconfig.json --noEmit`: aprovado.
- HTTP `GET /notas-fiscais-entrada`: `200`.
- HTTP para o alias historico antigo: `404`.
- HTTP `PATCH /contas-pagar/:id/programar-pagamento`: `404`.
- HTTP `PATCH /contas-pagar/:id/baixar`: `404`.
- HTTP `PATCH /contas-pagar/:id/pagar`: `404`.

## Evidencia de auditoria

- Busca textual pelo alias antigo em codigo, scripts, docs e checklist: sem ocorrencias.
- Busca textual pela rota canonica: ocorrencias esperadas em backend, frontend, smokes e documentacao.
- Busca por `DELETE` funcional em backend/frontend/migrations/scripts: sem endpoint de exclusao fisica nos modulos auditados.
- Busca por rotas de programacao, liberacao, pagamento e baixa: sem implementacao funcional desta etapa.

## Fora de escopo confirmado

- Sem SharePoint como banco.
- Sem Entra.
- Sem automacoes.
- Sem banco de producao.
- Sem programacao bancaria.
- Sem liberacao financeira.
- Sem pagamento.
- Sem baixa.
- Sem integracao bancaria, CNAB ou API bancaria.
- Sem `DELETE` fisico.
