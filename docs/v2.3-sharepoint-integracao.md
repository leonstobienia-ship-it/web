# V2.3 - Integração SharePoint

## Congelamento V2.2

A interface V2.2 homologada deve permanecer preservada. A V2.3 não redesenha telas, campos visuais ou fluxos do protótipo local.

Controle de versão local:

- Baseline protegido em `master`: `d8ab5a0`.
- Branch de trabalho: `dev/v2.3-sharepoint-integracao`.

Não foi criada tag retroativa da V2.2 porque não havia histórico Git anterior recuperável.

## Fonte de dados

- Fonte oficial V2.3: SharePoint via webpart SPFx.
- Protótipo HTML: permanece como demonstração V2.2 homologada.
- Integração concentrada em `src/webparts/enacSistema/services/SharePointEnacRepository.ts`.

## Persistência

- Solicitações são lidas da lista física `Lista 02 — Requisições de Compra`.
- Obras são lidas da lista física `Lista 01 - Controle de Obras ENAC`.
- Usuário autenticado é resolvido por `ENAC Usuarios Perfis.ContaMicrosoft365`.
- Alçadas são lidas de `ENAC Alcadas`.
- Snapshots são criados em `ENAC Snapshots Regras`.
- Snapshot de compra é vinculado em `Lista 02 — Requisições de Compra.SnapshotAprovacaoCompra`.

## Inventario readonly V2.3A

O inventario SharePoint readonly confirmou listas operacionais reais numeradas. Não criar `ENACObras` nem `ENACSolicitacoes`; usar o mapeamento consolidado em `sharepoint/mapeamento-listas-reais-v2.3.md`.

Provisionamento estrutural V2.3A concluído em 2026-06-04:

- `ENAC Usuarios Perfis`
- `ENAC Alcadas`
- `ENAC Historico Configuracoes`
- `ENAC Snapshots Regras`

O provisionamento criou/reaproveitou as quatro listas administrativas, ajustou seus campos `Title`, criou campos customizados e lookups e adicionou `SnapshotAprovacaoCompra` na `Lista 02 — Requisições de Compra`.

O campo `ENAC Alcadas.Obra` é lookup para `Lista 01 - Controle de Obras ENAC` / `NomedaObra`. O campo `SnapshotAprovacaoCompra` foi adicionado na `Lista 02 — Requisições de Compra` como lookup para `ENAC Snapshots Regras` / `Title`.

`ENAC Alcadas.TipoSolicitacao` deve ser `Choice` usando as choices reais da Lista 02: Material, Serviço, Equipamento, Ferramenta, Locação, Terceiro/Prestador, EPI, Documento/Taxa e Outro.

## Validacao residual da choice TipoSolicitacao

Leon executou manualmente o script readonly `scripts/sharepoint/04-validar-choice-tipo-solicitacao-readonly.ps1` apos a correcao da choice no SharePoint.

Resultado agregado:

- Lista validada por GUID: `Lista 02 — Requisições de Compra` / `0a204b87-b9a1-4d16-8654-55567a62ed01`.
- Campo interno lido: `TipodaSolicita_x00e7__x00e3_o`.
- Total de itens lidos: 6.
- `Documento?Taxa`: 0.
- `Documento/Taxa`: 0.
- Outros valores: 6.
- Vazio/nulo: 0.

Conclusao: nao existem registros residuais com `Documento?Taxa`; nenhuma migracao de itens e necessaria antes do provisionamento. A choice funcional vigente para `TipoSolicitacao` permanece `Documento/Taxa`.

`ENAC Snapshots Regras.Solicitacao` deve ser lookup para `Lista 02 — Requisições de Compra` usando o campo `ID`. O campo reverso `Lista 02 — Requisições de Compra.SnapshotAprovacaoCompra` deve apontar para `ENAC Snapshots Regras` usando `Title`.

URLs técnicas provisionadas para listas administrativas:

- `ENAC Usuarios Perfis`: `Lists/ENACUsuariosPerfis`
- `ENAC Alcadas`: `Lists/ENACAlcadas`
- `ENAC Historico Configuracoes`: `Lists/ENACHistoricoConfiguracoes`
- `ENAC Snapshots Regras`: `Lists/ENACSnapshotsRegras`

No snapshot inicial de aprovação de compra, não planejar campos `Cotacao` nem `PedidoCompra`. A regra nasce na requisição; vínculos posteriores podem ser avaliados em outra rodada.

`ENAC Usuarios Perfis` deve usar `PerfisAdicionais` como múltipla escolha; não usar o campo singular do desenho anterior.

`ENAC Historico Configuracoes` deve usar `Author` e `Created` nativos do SharePoint para autoria e data/hora. Não criar campos customizados para esses dados nesta rodada.

Todas as listas administrativas devem ter edição em grade desativada. Versionamento e bloqueio de edição em grade reduzem risco operacional, mas snapshots e histórico somente serão considerados efetivamente protegidos após definição/aplicação de permissões específicas.

Campos de moeda usam `LCID=1046` e `Decimals=2`. Identificadores internos únicos usam `Indexed=TRUE` e `EnforceUniqueValues=TRUE`. Campos `Note` devem ser texto simples sem rich text, `ContaMicrosoft365` deve aceitar uma pessoa apenas, e datas sem horário usam `Format=DateOnly`.

## Provisionamento estrutural concluído

Auditoria readonly posterior ao provisionamento confirmou:

- quatro listas administrativas encontradas com GUIDs e URLs técnicas corretas;
- versionamento ativo, anexos desativados e edição em grade desativada nas quatro listas;
- campos `Title` ajustados conforme esperado;
- campos customizados e lookups criados sem ausências ou divergências estruturais;
- `SnapshotAprovacaoCompra` existente na Lista 02 como lookup para `ENAC Snapshots Regras` / `Title`;
- nenhuma lista operacional foi recriada.

Próxima etapa: teste funcional controlado com dados mínimos, incluindo carga inicial de usuários/perfis, alçadas, snapshot e histórico. Não iniciar Power Automate antes dessa validação funcional.

## Teste funcional controlado V2.3B

Executado em 2026-06-04 com dados identificados pelo marcador `V2.3B-TESTE`, após conclusão do provisionamento estrutural V2.3A.

Auditoria readonly registrada em `sharepoint/auditoria-teste-funcional-v2.3b.md` confirmou:

- usuários/perfis de teste `usr-leon`, `usr-gustavo`, `usr-matheus` e `usr-kemilly`;
- alçadas `alc-v23b-teste-compra-ate-20000` e `alc-v23b-teste-compra-acima-20000`;
- snapshot `SNAP-V2.3B-TESTE-001`;
- histórico `V2.3B-TESTE`;
- Lista 02 validada por GUID `0a204b87-b9a1-4d16-8654-55567a62ed01`.

Resultado: V2.3B aprovada com ressalvas. O teste comprovou leitura/gravação controlada de dados administrativos e criação de snapshot. A ressalva é que o relatório local da auditoria não expõe explicitamente o item da requisição nem o valor do campo `SnapshotAprovacaoCompra`, embora a execução do script de teste tenha sido concluída por Leon antes da auditoria. Power Automate ainda não foi iniciado. A próxima etapa deve ser definida por Leon entre preparar integração funcional real/webpart e automação controlada.

## Resolução de alçada

1. Considerar apenas regras ativas e vigentes.
2. Filtrar por processo, tipo de solicitação, obra e valor.
3. Regra específica de obra prevalece sobre regra geral.
4. Regra ilimitada não usa valor máximo.
5. Sobreposições e lacunas devem ser detectadas antes do uso operacional.

## Substituição

A alçada não guarda substituto direto nesta rodada. O aprovador base vem da alçada; o aprovador efetivo é resolvido pelo cadastro do usuário:

- `SubstitutoTemporario`
- `InicioSubstituicao`
- `FimSubstituicao`

## Segurança

A V2.3 prepara a arquitetura para Power Automate e permissões reais, mas ainda não representa a segurança definitiva do fluxo. Alterações de usuários, alçadas, histórico e snapshots devem ser restringidas por permissões SharePoint.
