# V2.7A.5B - Ajuste CriarPedidoCompra Ao Schema Real Da Lista 03

Data: 2026-06-09

## Escopo

A rodada adaptou localmente `CriarPedidoCompra` ao resultado da auditoria readonly da `Lista 03 — Pedidos de Compra`, sem conectar ao SharePoint pelo Codex e sem executar escrita operacional.

Codex nao publicou pacote, nao instalou app, nao alterou tenant/listas/dados/permissoes, nao iniciou Power Automate e nao alterou `src/prototype/app.js`.

## Resultado Da Auditoria Readonly

- Lista: `Lista 03 — Pedidos de Compra`.
- GUID: `18ca132a-c36a-42aa-9968-d87ecd547a79`.
- Itens existentes no momento da auditoria manual: `2`.
- Campo de vinculo forte `SolicitacaoId`: nao confirmado / ausente.
- Campo textual confirmado para requisicao: `N_x00ba_daRequisi_x00e7__x00e3_o`.
- Obra obrigatoria: lookup `Obra`, enviado no REST como `ObraId`.
- Fornecedor obrigatorio: lookup `Fornecedor0`, enviado no REST como `Fornecedor0Id`.
- Status inicial confirmado: `Em elaboração`.

## Decisao Tecnica

Nao provisionar campo novo nesta rodada.

O pedido de compra de teste usa vinculo textual temporario:

`N_x00ba_daRequisi_x00e7__x00e3_o = <Title da requisicao>`

Para o item `11`, o valor esperado e `V2.7A-TESTE-001`, desde que esse seja o `Title` atual da requisicao.

Esse vinculo nao substitui um lookup forte definitivo. Ele e uma compatibilizacao temporaria com o schema real confirmado da Lista 03.

## Property Pane

Foram adicionadas propriedades de teste para a acao `CriarPedidoCompra`:

| Propriedade | Padrao | Uso |
| --- | --- | --- |
| `fornecedorTesteIdV27A` | vazio | ID obrigatorio do fornecedor na Lista 06, gravado em `Fornecedor0Id` |
| `statusPedidoInicialTesteV27A` | `Em elaboração` | Choice inicial de `StatusdoPedido` |
| `tituloPedidoTesteV27A` | vazio | Se vazio, gera `PED-V2.7A-TESTE-11-<timestamp>` |
| `descricaoPedidoTesteV27A` | `V2.7A-TESTE - pedido de compra controlado` | Gravado em `Descri_x00e7__x00e3_odoPedido` |
| `condicaoPagamentoTesteV27A` | vazio | Opcional, gravado em `Condi_x00e7__x00e3_odePagamento` |
| `prazoEntregaTesteV27A` | vazio | Opcional, gravado em `PrazodeEntrega` |

As flags de escrita permanecem desligadas por padrao.

## Pre-validacao Readonly

A pre-validacao de `CriarPedidoCompra` deve passar somente com:

- item `11` encontrado por GET na Lista 02;
- marcador `V2.7A-TESTE` confirmado;
- status atual `Aprovada para compra`;
- `SnapshotAprovacaoCompra` preenchido;
- valor de teste maior que zero, esperado `6720`;
- `ObraId` presente;
- descricao e centro de custo presentes para transporte;
- `StatusdoPedido = Em elaboração` existente nas choices confirmadas;
- `fornecedorTesteIdV27A` informado e resolvido por GET na Lista 06;
- nenhum pedido existente na Lista 03 pelo vinculo textual ou pelo prefixo `PED-V2.7A-TESTE-11`.

O diagnostico `CAMPO_VINCULO_TEXTUAL_USADO` foi tratado como informativo documental, nao como bloqueio automatico, porque a auditoria confirmou que a Lista 03 nao possui `SolicitacaoId` para uso nesta rodada.

## Payload Futuro Previsto

```json
{
  "Title": "PED-V2.7A-TESTE-11-<timestamp>",
  "N_x00ba_daRequisi_x00e7__x00e3_o": "V2.7A-TESTE-001",
  "ObraId": "<ObraId do item 11>",
  "Fornecedor0Id": "<fornecedorTesteIdV27A>",
  "ValordoPedido": 6720,
  "DatadoPedido": "<data atual ISO>",
  "StatusdoPedido": "Em elaboração",
  "CentrodeCusto": "V2.7A-TESTE",
  "Descri_x00e7__x00e3_odoPedido": "V2.7A-TESTE - pedido de compra controlado a partir da requisicao 11"
}
```

Nao enviar `SolicitacaoId`, `Fornecedor0` como texto, `Fornecedor` texto legado, campos derivados readonly de fornecedor/obra, campos de NF/pagamento/banco, `PATCH` ou `DELETE`.

## Execucao Preparada

Foi preparado `executarCriarPedidoCompraV27A()`.

Antes do POST, o metodo reexecuta a pre-validacao, relê a requisicao, confirma marcador/status/snapshot/obra, revalida fornecedor, confere pedido existente e apenas entao monta o POST da Lista 03.

Apos POST bem-sucedido, registra historico operacional com acao `CriarPedidoCompra`, item origem `Lista 02-11`, ID/titulo do pedido criado, vinculo textual usado, fornecedor ID, valor, status inicial, origem `Webpart V2.7A.5B` e sem Power Automate.

O status da requisicao nao e alterado nesta rodada.

## Limitacoes

- O vinculo com a requisicao ainda e textual e temporario.
- A execucao real depende de Leon informar `fornecedorTesteIdV27A` valido.
- A validacao funcional no tenant ainda nao foi executada.
- Power Automate continua fora do escopo.
- A V2.7A.5B nao representa homologacao de producao.
