# V2.7A.6 - Preparacao Do Teste Vincular Nota Fiscal

Data: 2026-06-09

Branch: `dev/v2.3-sharepoint-integracao`

## Status

Preparacao documental e de auditoria readonly da acao:

`VincularNotaFiscal`

A execucao permanece bloqueada. Codex nao conectou ao SharePoint, nao publicou pacote, nao instalou app no tenant, nao alterou listas/permissoes/dados, nao executou escrita operacional, nao executou teste funcional no tenant e nao iniciou Power Automate.

## Contexto Validado

A V2.7A.5C registrou manualmente:

- requisicao origem `Lista 02-11`;
- marcador `V2.7A-TESTE`;
- status da requisicao `Aprovada para compra`;
- snapshot preservado `3` / `SNAP-V2.7A-TESTE-11-20260609025123`;
- pedido criado na `Lista 03`, item `3`;
- numero da requisicao no pedido `V2.7A-TESTE-001`;
- fornecedor correto no lookup `Fornecedor0`, confirmado como `00.000.000/0001-00`;
- valor do pedido `6720`;
- status do pedido `Em elaboração`;
- historico criado;
- sem NF, sem pagamento e sem Power Automate.

## Lista 04 Localmente Identificada

O inventario local V2.3 identifica:

- lista: `Lista 04 - Notas Fiscais Recebidas`;
- GUID: `25aa4447-193d-418a-8e71-9bfd8e9995da`;
- URL: `/sites/Equipe.Obras/Lists/Lista 04  Notas Fiscais Recebidas`.

Campos relevantes vistos em inventario local antigo:

| Campo | InternalName | Tipo | Observacao |
| --- | --- | --- | --- |
| Nº do Pedido | `N_x00ba_doPedido` | Text | Possivel vinculo textual com pedido |
| Obra | `Obra` | Lookup | Lista 01 / `NomedaObra`; indicado como obrigatorio no inventario |
| Centro de Custo | `CentrodeCusto` | Text | Disponivel |
| CNPJ Fornecedor | `CNPJFornecedor` | Text | Disponivel |
| Fornecedor | `Fornecedor0` | Lookup | Lista 06 / `NomeFantasia`; indicado como obrigatorio |
| Nº da Nota Fiscal | `N_x00ba_daNotaFiscal` | Text | Disponivel |
| Série NF | `S_x00e9_rieNF` | Text | Disponivel |
| Data de Emissão | `DatadeEmiss_x00e3_o` | DateTime | Disponivel |
| Data de Vencimento | `DatadeVencimento` | DateTime | Disponivel |
| Valor Bruto da NF | `ValorBrutodaNF` | Currency | Disponivel |
| Tipo de NF | `TipodeNF` | Choice | Choices precisam de auditoria V2.7A.6A |
| Status da Conferência | `StatusdaConfer_x00ea_ncia` | Choice | Choices precisam de auditoria V2.7A.6A |
| Link da NF no SharePoint | `LinkdaNFnoSharePoint` | URL | Regra de obrigatoriedade precisa de auditoria |
| Anexos | `Attachments` | Attachments | Regra de uso/obrigatoriedade precisa de auditoria |

## Atualizacao V2.7A.6B

Leon executou a auditoria readonly da Lista 04 e os resultados foram registrados em:

- `reports/lista04-notas-fiscais-fields-readonly.json`;
- `reports/lista04-notas-fiscais-fields-readonly.md`.

A acao `VincularNotaFiscal` foi preparada no TypeScript para o schema real auditado, ainda sem execucao pelo Codex e sem teste funcional no tenant.

Campo obrigatorio adicional confirmado:

- `EnviadaparaContabilidade_x003f_` (`Enviada para Contabilidade?`), choice obrigatoria com valores `sim` e `não`.

Valor inicial adotado para o teste controlado:

- `não`.

Documento especifico da rodada:

- `docs/v2.7a6b-ajuste-vincular-nf-schema-lista04.md`.

## Pre-validacao Futura

A pre-validacao de `VincularNotaFiscal` deve usar somente GET e confirmar:

- pedido de origem existe na Lista 03, item `3`;
- pedido tem `N_x00ba_daRequisi_x00e7__x00e3_o = V2.7A-TESTE-001`;
- pedido tem status compativel para receber NF;
- pedido tem fornecedor preenchido;
- valor do pedido `6720`;
- item origem `11` continua `Aprovada para compra`;
- snapshot `3` permanece vinculado;
- Lista 04 mapeada por GUID `25aa4447-193d-418a-8e71-9bfd8e9995da`;
- campos obrigatorios da NF conhecidos: `Fornecedor0Id`, `ObraId`, `EnviadaparaContabilidade_x003f_`;
- status inicial da NF definido como `Recebida`;
- tipo inicial definido como `Material`;
- envio para contabilidade definido inicialmente como `não`;
- documento/anexo nao obrigatorio na criacao inicial;
- nao existe NF anterior de teste vinculada ao pedido;
- usuario/perfil tem permissao;
- historico previsto;
- `podeExecutar = sim` somente se tudo passar.

Alertas previstos:

- `LISTA04_NAO_MAPEADA`;
- `PEDIDO_TESTE_NAO_ENCONTRADO`;
- `PEDIDO_SEM_VINCULO_REQUISICAO`;
- `PEDIDO_STATUS_NAO_ELEGIVEL_PARA_NF`;
- `FORNECEDOR_PEDIDO_AUSENTE`;
- `CAMPOS_NF_OBRIGATORIOS_AUSENTES`;
- `STATUS_NF_INICIAL_NAO_MAPEADO`;
- `NF_TESTE_JA_EXISTENTE`;
- `PERFIL_SEM_PERMISSAO`;
- `CONFIRMACAO_NAO_CONFIGURADA`;
- `ERRO_REST_LISTA03`;
- `ERRO_REST_LISTA04`.

## Criterios De Parada

Parar se:

- a auditoria readonly da Lista 04 nao tiver sido executada/revisada;
- o pedido `3` nao existir ou nao for o pedido de teste;
- o pedido nao tiver fornecedor ou valor esperado;
- o item `11` ou snapshot `3` nao estiverem preservados;
- nao houver campo claro para vinculo NF-pedido;
- o status inicial da NF nao estiver mapeado;
- anexo/documento for obrigatorio e nao houver regra definida;
- existir NF de teste ja vinculada;
- aparecer `PATCH` ou `DELETE`;
- Power Automate for acionado.

## Fora Do Escopo

- Programar pagamento.
- Criar conta a pagar.
- Alterar status do pedido ou requisicao sem regra posterior documentada.
- Iniciar Power Automate.
- Liberar uso amplo em producao.

## Proxima Acao Manual

Leon deve publicar manualmente o pacote gerado, configurar o property pane com os parametros V2.7A.6B, executar primeiro a pre-validacao no tenant e somente acionar a escrita se `podeExecutar = sim`, sem alertas bloqueantes e com confirmacao manual exata.
