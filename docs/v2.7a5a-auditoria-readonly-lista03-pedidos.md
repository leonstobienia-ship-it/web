# V2.7A.5A - Auditoria Readonly Da Lista 03 Pedidos

Data: 2026-06-09

Branch: `dev/v2.3-sharepoint-integracao`

## Status

Preparada auditoria readonly da estrutura real da `Lista 03 — Pedidos de Compra`.

Codex nao conectou ao SharePoint, nao publicou pacote, nao instalou app no tenant, nao alterou listas/permissoes/dados, nao executou escrita operacional, nao executou teste funcional no tenant e nao iniciou Power Automate.

## Objetivo

Confirmar, sem escrita, os pontos que bloqueiam `CriarPedidoCompra`:

- campo real de vinculo com a solicitacao/requisicao da Lista 02;
- existencia ou ausencia de `SolicitacaoId`;
- campo correto de fornecedor;
- obrigatoriedade do fornecedor;
- choices reais de `StatusdoPedido`;
- status inicial recomendado do pedido de teste;
- campos obrigatorios para criar pedido;
- tipo de `ValordoPedido`;
- obrigatoriedade de `DatadoPedido` e `Title`;
- existencia de obra/centro de custo;
- existencia de vinculo com snapshot;
- possiveis validacoes que bloqueiem POST.

## Script Preparado

Arquivo:

`scripts/sharepoint/11-auditoria-lista03-pedidos-readonly.ps1`

O script:

- conecta ao site somente quando executado manualmente por Leon;
- le a lista pelo GUID `18ca132a-c36a-42aa-9968-d87ecd547a79`;
- le metadados de campos com `Get-PnPField`;
- destaca campos `Title`, `SolicitacaoId`, `Fornecedor`, `Fornecedor0`, `ValordoPedido`, `DatadoPedido`, `StatusdoPedido` e campos contendo `Solicit`, `Requis`, `Obra`, `Centro` ou `Snapshot`;
- gera `reports/lista03-pedidos-fields-readonly.json`;
- gera `reports/lista03-pedidos-fields-readonly.md`;
- nao le conteudo de itens;
- nao cria, altera ou remove nada.

Comandos de escrita SharePoint nao sao usados pelo script.

## Execucao Manual Sugerida

Exemplo:

```powershell
pwsh .\scripts\sharepoint\11-auditoria-lista03-pedidos-readonly.ps1 `
  -Tenant "enaccombr.onmicrosoft.com" `
  -ClientId "<app-id-autorizado>" `
  -AuthMode DeviceLogin
```

Leon deve revisar os relatórios antes de qualquer tentativa de escrita.

## Resultado Ainda Pendente

Como Codex nao executou o script nem conectou ao tenant, os campos abaixo continuam nao confirmados nesta rodada:

- campo real de vinculo com a Lista 02;
- existencia real de `SolicitacaoId`;
- fornecedor correto para escrita;
- obrigatoriedade real dos campos;
- choices reais de `StatusdoPedido`;
- existencia de pedido ja vinculado ao item `11`.

## Status De CriarPedidoCompra

`CriarPedidoCompra` continua bloqueado.

A acao so pode ser desbloqueada em rodada posterior, apos Leon fornecer o resultado readonly ou confirmar manualmente os campos da Lista 03.

## Proxima Etapa

V2.7A.5B deve registrar o resultado da auditoria readonly e, se o schema permitir, ajustar a pre-validacao/execucao de `CriarPedidoCompra` com os nomes internos reais.

