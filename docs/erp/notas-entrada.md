# Notas de Entrada ERP ENAC

Este documento permanece como alias historico. A documentacao canonica da V3.5A esta em:

```text
docs/erp/notas-fiscais-entrada.md
```

A rota canonica e:

```text
/notas-fiscais-entrada
```

O alias local `/notas-entrada` foi preservado apenas para compatibilidade com validacoes anteriores.
