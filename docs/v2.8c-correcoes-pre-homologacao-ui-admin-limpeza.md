# V2.8C - Correcoes Pre-Homologacao: UI, Administracao E Limpeza

Data: 2026-06-09

Branch: `dev/v2.3-sharepoint-integracao`

Referencia anterior: `8b38d75 style: melhorar identidade visual enac v2.8b-ui`

## Objetivo

Aplicar correcoes pre-homologacao apontadas por Leon, separando claramente:

- correcoes visuais aplicadas localmente;
- administracao de usuarios/alcadas preparada visualmente, sem escrita real;
- limpeza de dados apenas planejada por inventario readonly/dry-run;
- Power Automate fora do escopo.

## Correcoes Visuais Aplicadas

- `src/webparts/enacSistema/assets/enac-logo.png` substituido por PNG com canal alpha/transparencia.
- Cabeçalho compactado: menor padding, logo menor, subtitulo curto `Obras, compras, NF e financeiro`.
- Removida duplicidade de identidade no menu lateral: o logo e o texto `Sistema ENAC` aparecem apenas no cabeçalho.
- Menu lateral passou a exibir apenas `Módulos` e navegacao.
- Textos visiveis corrigidos com acentuacao: homologação, programação, aprovação, requisição, histórico, usuário, alçadas, cotações, liberação.
- Prefixos de teste removidos apenas visualmente:
  - `V2.3B-TESTE - `;
  - `usr-`;
  - `USR-`.

## Limpeza Visual De Prefixos

Foi criada funcao local de apresentacao:

- `formatDisplayName(value)`;
- `formatUserInternalId(value)`.

Essas funcoes sao usadas apenas no render. Elas nao alteram payload, nao alteram dados SharePoint, nao alteram historico gravado e nao modificam modelos.

Valores tecnicos permanecem preservados nas fontes de dados e no painel tecnico quando aplicavel.

## Administracao De Usuarios

Tela `Usuários` recebeu preparacao visual para:

- visualizar perfil atual;
- visualizar status;
- exibir controles de futura alteracao de perfil;
- exibir controles de futura alteracao de status.

Os controles ficam desabilitados nesta rodada.

Motivo: a escrita administrativa ainda depende de:

- auditoria readonly do schema real;
- confirmacao de InternalNames e choices;
- pre-validacao;
- confirmacao manual;
- historico;
- autorizacao explicita de Leon em rodada propria.

Acao futura proposta: `AtualizarUsuarioPerfilStatus`.

## Administracao De Alcadas

Tela `Alçadas` recebeu preparacao visual para:

- visualizar aprovador sem prefixos de teste;
- visualizar faixa atual;
- exibir previa de limite/liberacao.

Os controles ficam desabilitados nesta rodada.

Motivo: a escrita de alçada por usuario ainda depende de:

- confirmar se a regra e por usuario, perfil ou regra global;
- confirmar campos reais da lista `ENAC Alcadas`;
- validar impactos de sobreposicao/lacuna;
- pre-validacao;
- confirmacao manual;
- historico.

Acao futura proposta: `AtualizarAlcadaUsuario`.

## Limpeza De Dados

Nenhuma limpeza, delecao ou alteracao foi executada.

Foi criado script readonly:

`scripts/sharepoint/14-inventario-limpeza-dados-teste-readonly.ps1`

O script gera, quando executado manualmente por Leon:

- `reports/v2.8c-inventario-limpeza-candidatos.md`;
- `reports/v2.8c-inventario-limpeza-candidatos.json`.

Classificacoes previstas:

- candidato a limpeza apos confirmacao;
- manter como evidencia;
- cancelar/inativar em vez de excluir;
- nao mexer.

## Preservacao Funcional

Nao houve alteracao intencional em:

- regras de negocio;
- payloads;
- chamadas REST;
- services SharePoint;
- fluxos V2.7A;
- travas V2.6A/V2.7A;
- scripts de escrita;
- `src/prototype/app.js`;
- backup V2.2.

Power Automate nao foi iniciado e segue fora do escopo.

## Validacao Manual Posterior

Leon deve validar visualmente:

- logo sem caixa branca;
- cabeçalho mais compacto;
- identidade aparecendo apenas uma vez;
- acentuacoes corrigidas;
- usuarios e alçadas sem prefixos visuais indesejados;
- controles administrativos desabilitados ate rodada propria;
- nenhuma limpeza executada.
